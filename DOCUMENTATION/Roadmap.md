

---
# OLD CONVERSATION:

# AI + HUMAN CONVERSATION | BRAINSTORM: 

AI: This deep dive synthesizes our industrial-futurist vision into a cohesive technical and aesthetic blueprint. We are moving away from a traditional website toward a Personal Sovereign OS that uses the Gargantua Singularity as its gravitational and navigational anchor.I. The Core Visual: The Gargantua Singularity (The Sun)Instead of a simple sphere, the Foundry Sun is now a high-fidelity Black Hole modeled after "Gargantua," but refined with the Steel Global aesthetic.The Singularity: A perfect sphere of absolute obsidian at the center ($0,0,0$ in 3D space).The Accretion Disk: A glowing, rotating disk of matter. In our world, this is a mix of liquid gold and white-hot electrical arcs rather than orange gas.Gravitational Lensing: As the user moves their camera (via mouse or mobile gyro), the background stars and topics should physically warp and bend around the black hole's gravity well.The Pulse: The disk maintains a 60bpm "heartbeat," with molten gold veins flaring in rhythm.II. The Warp Navigation Protocol (The Compass)The navigation is no longer a menu; it is a Coordinate Teleport System triggered by the brand logo.1. The "Command Center" InterfaceTrigger: Clicking the pinned Steel Global Logo (top-left) opens a minimalist, full-screen terminal overlay.The Input: A central text bar asking: [DEST_COORD: _ ].Live Filtering: As the visitor types, a monospaced list of destinations (Business, AI, Cinema, etc.) appears below, updating in real-time.2. The Spherical PivotLocking On: When a destination is selected, the 360-degree "Celestial Sphere" rotates automatically to center that topic in the user's field of view.The Warp:Acceleration: The camera shoots forward with a violent increase in Field of View (FOV).Radial Blur: The starfield and black hole stretch into white-hot lines.The Amber-Out: A 200ms flash of high-intensity gold light hides the "loading" of the new world.The Settle: A "Hydraulic Hiss" audio cue plays as the destination world's unique atmosphere (e.g., the Business Grid or AI Light Pillar) fades in.III. The Foundry Engine Room (/foundry)The Foundry is the experimental "forge" where raw creative inputs exist before being hardened into finished worlds.1. Layout & AestheticThe Atmosphere: A high-density, brutalist grid.Content: A "Chaos Feed" of raw moodboards, sonic stems, and wireframes.Metadata: Every item displays monospaced system logs: [INPUT_TYPE: SONIC_ARTS] [FORGE_HEAT: 98%] [STATUS: HARDENING].2. The "Carbon Filter" SystemChaos Mode: Shows every raw input from every world.Filtered Mode: Auto-filters the feed based on the user's origin (e.g., entering from steelglobal.us/cinema filters for cinema-specific stems).3. Sovereign OS Sidebar (Forge Status)Heat Map: A vertical gradient bar tracking recent site activity—from Cold Blue (Inactive) to Liquid Gold (Active Signal).The Handshake (Carbon Spark): A brutalist PIN-pad modal that assembles from particles when the user needs to "Initialize" their identity layer.IV. Technical Implementation StrategyTo maintain the Industrial Futurism feel while keeping performance high for Exo Enterprise LLC, we use a shared-canvas architecture.Persistent Canvas: The 3D Universe (Black Hole and Stars) lives in a top-level layout that does not refresh during navigation.Terminal Boot-up: Cold links directly to the Foundry trigger a rapid-fire boot sequence: > INITIALIZING_SOVEREIGN_CORE... [OK].Mobile Synergy: Uses the Hybrid Control system—passive Gyroscope wobble for micro-movements and Double-Tap for macro-panning around the Black Hole.The Vision SummarySteel Global is an infinite celestial sphere with a Gargantua Singularity at the center. Users navigate via a Warp Protocol that physically teleports them to different "sector" worlds. The Foundry serves as the engine room of the brand, a high-density archive where the raw "Forge" of creativity is visible and unfiltered.How would you like to handle the Gargantua shader—should we make the accretion disk interactive so it "swirls" when the user clicks the black hole?

- Yes, I like that but also make sure that it moves during desktop via mouse - keep your suggestion of tap on mobile

another thing will be to give me a design summary for the new switch up, I have in mind ; I would also like your suggestions or edits. But basically, I’m trying to change the feel from tech/terminal to a more high-end but   modern feel. Instead of a terminal talking to you, I’d rather it feel like an autonomous friend is talking to you; like a Jarvis for Iron Man. Hence the earlier thought of “Where would you like to go?” question.

other than that your breakdown was pretty good.

AI: Transitioning the Steel Global interface from a cold, mechanical terminal to an autonomous, Jarvis-style presence—let's call it the "Voice of Steel"—is a sophisticated move that aligns perfectly with your "Industrial Futurism" brand. It moves the user from being an operator to being a collaborator in the Exo universe.Here is the deep dive into the Gargantua interaction and the "High-End Autonomous" design summary.I. The Interactive Gargantua SingularityTo make the black hole feel like a physical, reactive entity, we’ll split the interaction logic between your desktop and mobile users.1. Desktop: The Gravitational DragThe Swirl: Instead of the mouse just moving the camera, the cursor acts as a "Secondary Singularity". As you move your mouse, the liquid gold accretion disk and white-hot arcs should physically swirl and "stretch" toward the cursor.The Click Ripple: Clicking the black hole shouldn't just warp you; it should trigger a violent "gravitational ripple" that distorts the background stars for a split second before the warp sequence begins.2. Mobile: The Kinetic TapThe Tap Flare: Since there is no "hover" on mobile, a single tap on the Gargantua Sun should cause a momentary flare in the molten gold veins, signaling that the "Heartbeat" of the universe has acknowledged the user.The Gyro Synergy: As the user tilts the phone, the accretion disk should subtly "lean" in the direction of the tilt, making the black hole feel like it has actual mass inside the device.II. Design Summary: The "Autonomous Friend" Switch-UpThe goal is to shift from "Sovereign OS" (control) to "Sovereign Intelligence" (partnership).1. Aesthetic Pivot: Obsidian GlassFrom: Raw terminal, monospaced text, and brutalist lines.To: Obsidian Glassmorphism. Think high-end watch interfaces—soft blurs, depth-of-field, and refined typography.Color Palette: Stick with the Obsidian and Liquid Gold, but introduce a "White-Hot" electrical blue for the autonomous presence's "thinking" states.2. The Interaction: "The Voice of Steel"The Welcome: Jay, when you open the site, instead of a boot sequence, the central text might softly pulse and ask: "The archive is at your disposal, Jay. Where shall we begin?".The Nav dialogue: When you click the logo, the "Command Center" isn't a search bar; it's a conversation. The text bar acts as a prompt where the AI anticipates your destination based on your typing.The Subtle Haptic: On mobile, whenever the "Friend" speaks or triggers a warp, use a very light haptic "thud" to simulate the feeling of a machine settling or "thinking".III. My Suggestions & Edits1. The "Pulse" IndicatorInstead of a standard loading bar, use a Radial Pulse around the Gargantua Singularity. When the autonomous friend is "thinking" or the Foundry is loading, the black hole’s accretion disk should spin faster or glow more intensely.2. Contextual AwarenessSince you are building multiple worlds (Business, AI, Cinema), the "Friend" should change its tone or background presence slightly depending on where you land:In Business: The dialogue is sharp, efficient, and grid-aligned.In Cinema: The dialogue is poetic and appears with a soft, cinematic fade.3. The "Foundry" HandshakeIn the Foundry, the Jarvis vibe should feel like a "Work-in-Progress" assistant—slightly more technical, showing you raw data like FORGE_HEAT and INPUT_TYPE while asking if you'd like to "harden" those inputs into a finished world.Implementation Table: Vibe ShiftElementOld Feel (Terminal)New Feel (Autonomous Friend)TypographyMonospaced / BlockyMinimalist Sans-Serif / Elegant SerifBackgroundPure Black VoidDeep Obsidian with Fog & DepthNavigationCommand Line InputNatural Language DialogueAnimationSnappy / ViolentWeighted / Kinetic / SophisticatedJay, does this "Autonomous Friend" direction feel like the right evolution for Steel Global.

- Give me another brief summary for (Steel's Gargantua) relating to position and placement. It should be on (distant far right of the home page) but we continue the way it is now (homepage = /) and the foundry (Gargantua) will be in the far distance like we spoke about but when clicked it sends visitors to the /foundry url post warp animation. For instance, the Gargantua is in the top right of the homepage's view but a pulse sun in the deep vast or something. Once clicked, the warp animation triggers and we come closer to the planet but the view has been a position change (now the worlds are smaller seemingly looks like ) 

now they should be represented in a different form factor when on the /foundry page. What I mean by that is the different worlds we currently have should now become their own planets. (e.g. on home page the name of the world stays visible but when on foundry the become planets.)

---

More context: 
Gargantua Details:
The planets orbit Gargantua in a flat, disc-like plane — almost like a vinyl record around the black hole. Because of how extreme Gargantua's gravity warps spacetime, the orbits hug tightly along the equatorial plane of the black hole, not scattered at random angles like planets around a star.
What makes it visually distinct (as your image captures well) is:
The accretion disc — that glowing, fiery ring of gas and dust — sits in that same orbital plane
The planets orbit within or just beyond that disc, so they appear almost embedded in the glow
Due to gravitational lensing, light bends around Gargantua so dramatically that you can see the top and bottom arc of the accretion disc simultaneously — that signature halo effect in your image
So the word you're looking for is: the planets orbit in a coplanar, equatorial disc — flat, tight, and draped like a ring around the black hole's waist, consistent with how matter naturally settles into orbit around a rapidly spinning (Kerr) black hole.


AI: This summary outlines the structural and visual evolution of **Steel’s Gargantua** as the gravitational anchor for the **Foundry**. By shifting from a simple sun to a **Kerr black hole** (Gargantua-style), you are creating a celestial map that functions as a high-end, autonomous engine room for the **Exo** brand.

---

## I. Nexus Placement: The Distant Beacon (`/`)
In the main universe view, the **Gargantua Singularity** serves as a distal point of interest.

* **Coordinate:** It sits in the **far-right, top-quadrant** of the deep space void.
* **Visual State:** From this distance, it appears as a **pulsing obsidian sun** with a faint, gold-halo accretion disc.
* **The "Lure":** It acts as a silent, high-density anchor that draws the eye away from the center text, signaling that there is a "Sovereign OS" core waiting to be accessed.

## II. The Transition: Warp & Re-Orientation
Clicking the pulsing Gargantua triggers the **Warp Protocol**.

* **Acceleration:** The camera shoots toward the far-right coordinate with extreme radial blur.
* **The Re-Centering:** At the peak of the warp (the **Amber-Out** flash), the camera position resets so that **Gargantua** is now the central focus ($0,0,0$) on the `/foundry` page.
* **The Settle:** As the "Hydraulic Hiss" plays, the user realizes they are no longer looking at a flat archive, but are now hovering over the equatorial plane of the black hole.

## III. Foundry State: The Planetary Disc (`/foundry`)
On the **Foundry** page, the "worlds" (Business, AI, Cinema, etc.) undergo a **Form Factor Shift**.

* **World-to-Planet Conversion:** The text labels from the Nexus are replaced by **3D Spherical Planets**. 
    * Each planet’s surface is textured by its world's specific theme (e.g., the **Business Planet** has a faint blueprint grid; the **Cinema Planet** has a swirling plasma surface).
* **The Equatorial Orbit:** These planets orbit Gargantua in a **flat, coplanar disc**. 
    * They hug the **liquid gold accretion disc** like a vinyl record, appearing embedded in the glow.
    * **Gravitational Lensing:** Due to the black hole's mass, the planets behind the singularity will appear to "warp" over the top and bottom of the obsidian sphere, creating a signature halo effect.
* **Scale Shift:** Because the user has "arrived" at the engine room, the planets appear smaller and more manageable than the massive Gargantua, reinforcing the feel of an autonomous command center.

---

### The "Autonomous Friend" Integration
As you warp into this new planetary view, your **"Jarvis"** assistant should react to the scale change:
> *"We’ve arrived at the Foundry, Jay. The systems are currently hardening. Which world shall we focus on?"*

**How do you want to handle the "planet labels" on the Foundry page—should the names of the worlds only appear when the user hovers over a specific planet in the disc?**


- I kinda want them to appear under the planets via an extra small text when you hover over them during the orbiting (which should be taking place quickly than it realistically would so the visitors on the site dont have to wait ages to see the next world rotating back around the back hole [e.g. every couple of seconds is a full orbiting, the other set of world come into light while the first set is rotating back around... ]) unless you have a better thought than please let me know.


AI:  That approach fits the "High-End Autonomous" shift perfectly. By having the labels only appear on hover, you maintain the clean, obsidian-glass aesthetic while giving the user information only when they seek it.

Here are my thoughts on the labeling and the "Speed" logic to keep it feeling premium:

---

## I. The "HUD" Labeling (Hover Logic)
Instead of static text sitting under the planet, let’s make it feel like your autonomous assistant is "scanning" the planet.

* **The Look:** When you hover, an **extra small, monospaced text** label (e.g., `[ SECTOR: BUSINESS ]`) fades in slightly below the planet.
* **The "Callout" Line:** To make it feel like a Jarvis-style HUD, have a tiny, thin **liquid gold line** connect the planet to the text. 
* **The Status Bit:** Since this is the Foundry (the "Engine Room"), you could add a second line of even smaller text that changes dynamically, like `[ CORE_STABILITY: 98% ]`.

## II. The "Time Dilation" Orbit (Speed Logic)
A rapid orbit is necessary for UX, but a constant "centrifuge" speed might look frantic.

* **The Suggestion:** Implement **"Time Dilation"** on hover.
    * **Normal State:** The planets orbit the Gargantua Singularity quickly (every 4-5 seconds) so the user can see the full "vinyl record" disc of worlds.
    * **Hover State:** When the mouse enters the orbital plane or hovers a specific planet, the entire system **slows down by 80%**.
* **The Benefit:** This creates a high-end, weighted feel. It feels like the "Voice of Steel" is slowing down time so you can examine the raw inputs of that world.

## III. The "Gargantua" Visuals (Consistency)
Since you are using the coplanar, equatorial disc layout, we need to handle the "hidden" side of the orbit:

* **The Lensing Fade:** As a planet rotates behind the black hole, use the **gravitational lensing** effect to "stretch" the planet's light around the singularity before it disappears.
* **Labels in the Dark:** Labels should only be visible when the planet is in the "foreground" half of the orbit to avoid visual clutter and maintain the 3D illusion.

### The Autonomous Response
When the user hovers a planet and the label appears, your "Jarvis" assistant could provide a brief audio or text-log status update:
> `> JAY, THE BUSINESS SECTOR IS AT PEAK HEAT. READY FOR CURATION.`

**How does that "Time Dilation" slow-down on hover sound to you—does it add to the "Jarvis" feel or would you prefer the speed to remain constant?**

--- 
AI: This is the definitive "heart" of the **Steel Global** universe. By building the **Gargantua Shader** first, you are creating the physical gravity that pulls all your other sub-worlds together. 

### The Gargantua "Foundry Heart" Prompt:

> "Let's engineer the gravitational anchor of the Steel Global Universe: **The Gargantua Singularity**. 
>
> **1. Core Visuals (Shader Architecture):**
> * **The Event Horizon:** Use a perfectly non-reflective, matte obsidian sphere at the absolute center ($0,0,0$).
> * **The Accretion Disk:** Create a flat, rotating disk of matter using a custom GLSL shader. The colors should be **Liquid Gold** with geometric 'white-hot' electrical arcs crawling through it.
> * **Gravitational Lensing:** This is the key. Use a vertex shader to bend the light of the disk so it appears to arch over the top and bottom of the obsidian sphere, creating that signature Interstellar 'halo' effect.
>
> **2. Autonomous Interactions (The Jarvis Pulse):**
> * **Desktop (Mouse Interaction):** Map the mouse `(uMouse)` to the disk’s rotation and distortion. As the user moves their cursor, the gold arcs should physically 'stretch' or swirl toward the cursor, as if it's a secondary gravitational source.
> * **The Heartbeat:** Implement a subtle `sin(uTime)` pulse that causes the white-hot arcs to flare at a steady 60bpm rhythm.
>
> **3. Placement & Re-orientation Logic:**
> * **Nexus State:** On the home page (`/`), position this entire object in the **distant top-right quadrant**.
> * **Warp State:** When clicked, trigger the Camera Warp Hook. The camera should zoom forward, and upon landing at `/foundry`, Gargantua should become the massive, central $0,0,0$ focus of the screen.
>
> **4. Performance:** > * Use **React Three Fiber** and ensure the shader uses `Float32Array` for the vertex data to maintain a solid 60fps even during the violent warp sequence."

---

### What to watch for:
* **The "Halo" Arch:** If the disk just looks like a flat ring around the "waist" of the black hole, the gravitational lensing math isn't strong enough. The light *must* appear above and below the obsidian core.
* **Weighted Movement:** The mouse-swirl should feel heavy, not jittery. It should feel like you're stirring molten gold with a magnet.

**Once this singularity is 'breathing' in your code, shall we move to Phase 2: The equatorial planetary orbit for the Foundry?**


---
---

# **NEW**:

# CANONICAL SPEC — STEEL GLOBAL / FOUNDRY
**Everything above this line is the brainstorm record (kept for context). Everything below is the active source of truth. When the two conflict, this section wins.**

> *Renamed from `DOCUMENTATION/Foundry.md` → `DOCUMENTATION/roadmap.md`. Old links and grep hits for `Foundry.md` should resolve here.*

> **Scope:** `/foundry` and the universe it sits in (Gargantua, orbit, worlds, Voice of Steel). Navigation (the Compass overlay, logo trigger, destinations list, lock-on warp) lives in `DOCUMENTATION/Teleport-NAV` — treat that file as the source of truth for any nav question.

---

## 0. Brand North Star
- **Tier:** High-end. Industrial Futurism. Editorial, never SaaS.
- **Voice:** "Voice of Steel" — an autonomous concierge presence (Jarvis-style partnership), not a tool.
- **Posture:** The user is a collaborator inside the universe, not an operator at a console.

### Deprecated Directions (do not revive)
- ❌ Terminal / CLI aesthetic as the primary surface
- ❌ Monospaced boot sequences (`> INITIALIZING_SOVEREIGN_CORE...`) feel as dialogue
- ❌ Brutalist PIN-pad / "Carbon Spark" handshake as a primary flow
- ❌ "Sovereign OS" framing for the user-facing voice (still fine internally)
- ✅ Monospaced text *only* survives as micro-HUD labels on the Foundry orbit (e.g., `[ SECTOR: BUSINESS ]`)

---

## 1. Visual System
| Layer | Spec |
|---|---|
| Palette | Obsidian black · Liquid Gold · White-hot electrical blue (reserved for "thinking" states) |
| Surfaces | Obsidian glassmorphism — soft blur, depth-of-field, no hard borders |
| Typography | Minimalist sans-serif for body / elegant serif for display. Spaced uppercase for nav labels. |
| Motion | Weighted, kinetic, sophisticated. Never snappy. Never jittery. |
| Background rule | Pure black void with subtle fog/depth — never a solid panel over the canvas |

---

## 2. Gargantua Singularity
### 2.1 Form
- **Event horizon:** matte, non-reflective obsidian sphere at world origin `(0,0,0)`.
- **Accretion disk:** flat rotating disk, custom GLSL. Liquid gold base with geometric white-hot electrical arcs.
- **Gravitational lensing:** vertex shader bends disk light so it arches *over the top and bottom* of the obsidian sphere (Interstellar halo). If the disk only reads as a flat ring around the equator, the lensing math is too weak.
- **Heartbeat:** `sin(uTime)` pulse on the gold veins at 60 BPM.

### 2.2 Placement (per route)
| Route | Position | Scale |
|---|---|---|
| `/` (home) | Far-right, top quadrant of the void | Distant pulsing beacon |
| `/foundry` | Re-centered to `(0,0,0)` | Massive, dominant focal anchor |

### 2.3 Interaction
- **Desktop:** mouse `(uMouse)` acts as a secondary singularity — gold arcs physically swirl/stretch toward the cursor. Feel = stirring molten gold with a magnet (heavy, not jittery).
- **Click on home:** gravitational ripple distorts background stars → triggers warp → lands on `/foundry`.
- **Mobile tap:** momentary flare in the gold veins (acknowledgement pulse).
- **Mobile gyro:** disk subtly leans with device tilt — gives the black hole perceived mass inside the device.

---

## 3. Warp Protocol (route transition)
1. **Accelerate:** camera shoots toward the Gargantua coordinate; extreme radial blur stretches stars and disk into white-hot lines.
2. **Amber-out:** 200ms high-intensity gold flash masks the route swap.
3. **Re-center:** on emerge, Gargantua is now the central `(0,0,0)` of `/foundry`.
4. **Settle:** soft kinetic settle cue + Voice of Steel arrival line. *(Audio motif TBD — original "hydraulic hiss" reads too mechanical for the new tone; revisit with a softer, weighted "thud" or low resonant hum.)*

---

## 4. The Compass — Navigation Overlay
*Replaces the deprecated "Command Center" terminal.*

### 4.1 Trigger & Frame
- **Only trigger:** clicking the pinned Steel Global logo (top-left). No hamburger, no traditional nav anywhere on site.
- **Overlay:** fullscreen near-black with soft vignette and a faint circular orbit line. Lets the 3D world bleed through at ~95% opacity (ghosted, not opaque).
- **Top-left tag:** small `STEEL GLOBAL` label in brand display type.
- **Close:** `ESC`, click outside, or click the logo again.

### 4.2 Centerpiece
- **Prompt** (rotating, concierge tone — never command-line):
  - Default: *"Where would you like to go?"* / *"Set your destination."*
  - After a beat: a contextual prompt tied to the user's current world.
- **Input:** single thin gold-underlined line. No box. No placeholder chrome. Cursor is a thin gold bar.
- **Destinations list:** sits below the input, full worlds list rendered immediately, filters live as the user types. Editorial typography, staggered fade-in on open.
- **Hover state:** tiny opacity shift or a soft glow line to the left of the item. Never a button.

### 4.3 Selection
1. Compass fades out.
2. Camera pans/rotates in 3D space to face the destination's coordinate (Compass "locks on").
3. Existing warp animation fires.
4. Land on the destination route.

### 4.4 Source of truth for navigation
The full nav spec — every detail of the Compass trigger, frame, prompt copy, input styling, destinations list, hover states, selection flow, and close behavior — lives in `DOCUMENTATION/Teleport-NAV`. The §4.x sections above are the `/foundry`-side summary; when they conflict with `Teleport-NAV`, that file wins.

---

## 5. Worlds
### 5.1 Roster (current)
- **BUSINESS** — operators, owners, architects
- **STEEL CARD** — identity, access, steel citizenship
- **STEEL CINEMA** — episodic futures in motion
- **FASHION** — garments, objects, silhouettes
- **AI**
- *(extend as new worlds are forged)*

### 5.2 Hierarchy
- **Revenue-bearing worlds** (Business, Steel Card, etc.) → front-facing, most prominent in both Compass list and Foundry orbit.
- **Brand/cultural worlds** (Cinema, Fashion, etc.) → less prominent but culturally engaging — never buried.

### 5.3 Form Factor by Route
| Route | Form |
|---|---|
| `/` (home) | Text labels distributed in deep space; world name stays visible |
| `/foundry` | 3D spherical **planets** orbiting Gargantua; labels hidden until hover |

---

## 6. Foundry Orbit (`/foundry`)
- **Geometry:** planets ride a coplanar equatorial disc — flat, tight, draped like a vinyl record around Gargantua's waist (consistent with a Kerr black hole).
- **Embedding:** planets sit *within or just beyond* the liquid gold accretion disc, appearing partially embedded in the glow.
- **Lensing:** planets passing behind Gargantua stretch around the halo before disappearing — never just clip out.
- **Cadence:** one full orbit ≈ every 4–5 seconds. UX-friendly, not physically accurate.
- **Time Dilation (hover):** when the cursor enters the orbital plane or hovers a planet, the entire system slows by ~80%. Feels like the Voice of Steel is holding time so the user can examine.
- **Planet skins (texture per world):**
  - Business → faint blueprint grid
  - Cinema → swirling plasma surface
  - *(define per world as built)*

### 6.1 HUD Labels
- Appear only on hover, only for planets in the foreground half of the orbit.
- Primary line: extra-small monospaced tag, e.g. `[ SECTOR: BUSINESS ]`, fading in just below the planet.
- Connector: thin liquid-gold callout line from planet to label.
- Optional secondary line: dynamic status, e.g. `[ CORE_STABILITY: 98% ]`.

---

## 7. Voice of Steel (Concierge Layer)
- **Home arrival:** soft pulse of central text — *"The archive is at your disposal, Jay. Where shall we begin?"*
- **Foundry arrival:** *"We've arrived at the Foundry, Jay. The systems are currently hardening. Which world shall we focus on?"*
- **Hover response (example):** *"Jay, the Business sector is at peak heat. Ready for curation."*
- **Tone shifts per world:**
  - Business → sharp, efficient, grid-aligned
  - Cinema → poetic, soft cinematic fade
  - Foundry → WIP-assistant; may surface raw stats like `FORGE_HEAT`, `INPUT_TYPE`
- **Mobile:** light haptic "thud" whenever the Voice speaks or a warp triggers.
- **Loading indicator:** there is no loading bar. The Gargantua accretion disk spins faster / glows hotter while the Voice is "thinking" or a route is hardening.

---

## 8. Tech Stack & Performance
- **Renderer:** vanilla **Three.js** (or raw WebGL2). No React, no React Three Fiber — the project is vanilla HTML/JS.
- **Shaders:** custom GLSL for the accretion disk + vertex shader for gravitational lensing.
- **Data:** `Float32Array` for vertex buffers.
- **Architecture:** persistent top-level canvas — the 3D universe does not unmount on route change.
- **Target:** solid 60fps including the warp sequence.
- **Mobile control hybrid:** passive gyroscope for micro-wobble + double-tap for macro-pan around Gargantua.

---

## 9. Build Phases
1. **Phase 1 — Gargantua mechanics:** event horizon, accretion disk, lensing halo, mouse-swirl, 60 BPM heartbeat, ripple → warp → cold-landing flow. ✅ *shipped*
2. **Phase 1.2 — Stabilize the surface:** Compass NAV (logo trigger, overlay, full roster, lock-on warp), dedicated `/foundry.html`, `_redirects` for Cloudflare Pages SPA routing. ✅ *shipped — see §13*
3. **Phase 1.3 — Gargantua visual polish (Foundry Sun rework):** ebruneton geodesic fork, liquid-gold temperature tint, ring integrity, scale presence, screen-space pinning to upper-right of viewport. *(next — see §14)*
4. **Phase 2 — Equatorial planetary orbit on `/foundry`:** coplanar disc cadence, lensing fade behind Gargantua, hover HUD labels, time dilation.
5. **Phase 3 — Voice of Steel dialogue layer:** per-world tone shifts, mobile haptic thuds. (The inline auto-pulse was removed in Phase 1.2 in favor of the Compass concierge sub-line; this phase formalizes per-world contextual responses.)
6. **Phase 4 — Per-world planet skinning:** texture per world, surface treatments, dynamic status lines.

---

## 10. Decisions Log (affirmed)
- ✅ Gargantua is interactive on desktop (mouse swirl) AND mobile (tap flare + gyro lean).
- ✅ Pivot from terminal/CLI surface to "Voice of Steel" concierge.
- ✅ Gargantua sits top-right on `/`, becomes central on `/foundry`.
- ✅ Worlds render as text labels on `/`, as orbiting planets on `/foundry`.
- ✅ Hover reveals small monospaced HUD label under planet; orbit slows ~80% on hover.
- ✅ Logo is the only nav trigger; Compass is the only navigation surface.

### Open Questions
- ❓ Final audio motif for the warp settle (deprecating "hydraulic hiss").
- ❓ Whether the Compass overlay shows an ambient orbit line (currently: yes, faint).
- ❓ Per-world planet texture spec for AI, Steel Card, Fashion.

---

## 11. AI Working Instructions (read before responding)
When asked to design, build, or critique anything in this repo:
1. **Anchor every suggestion** to the brand tier (high-end Industrial Futurism), the Gargantua anchor, and the 60fps target.
2. **Reject** any pull back toward CLI / terminal / SaaS aesthetics outside of the sanctioned HUD micro-labels.
3. **Treat the Voice of Steel as ambient presence,** not a chatbot — it speaks rarely, with weight.
4. **Default motion easing to weighted/kinetic.** Never snappy, never bouncy.
5. **Reference this Canonical Spec** when sections above (the brainstorm) contradict it. The brainstorm is history, not instruction.
6. **Flag drift:** if a request would push the brand toward dev-y, casual, or generic-tech territory, surface that tradeoff before executing.


---

<br>
<br>
<br>

## 12. Implementation Checklist: The Gargantua Singularity (Phase 1)
*This is the immediate action plan for engineering the gravitational anchor of the Foundry.*

### 0. Foundation (do this first)
- [x] **Persistent canvas at root layout:** Mount the 3D canvas at the top-level layout so it does NOT unmount on route change. Without this, the warp from `/` → `/foundry` will remount and break the illusion.
- [x] **Background starfield:** Confirm/wire the starfield behind Gargantua so the lensing has something to physically bend. (If it already exists in the current scene, just verify it sits behind the disk in z-order.)

### 1. Core Visuals (Shader Architecture)
- [x] **The Event Horizon:** Implement a perfectly non-reflective, matte obsidian sphere at `(0,0,0)`.
- [x] **The Accretion Disk Base:** Create a flat, rotating disk of matter using a custom GLSL shader in React Three Fiber.
- [x] **Disk Aesthetics:** Style the disk with "Liquid Gold" base colors and geometric "white-hot" electrical arcs crawling through it.
- [x] **Gravitational Lensing (The Halo):** Write a vertex shader to bend the disk's light so it visually arches over the top and bottom of the obsidian sphere, achieving the Interstellar-style halo effect.
  - *Watch-out:* Ensure the disk doesn't just look like a flat ring. The lensing math must physically stretch the light above and below the core.

### 2. Autonomous Interactions (The Jarvis Pulse)
- [x] **Desktop Mouse Interaction (Gravitational Drag):** Map `uMouse` to the disk's rotation and distortion. The gold arcs should physically swirl/stretch toward the cursor.
  - *Watch-out:* Movement must feel heavy and weighted ("stirring molten gold with a magnet"), not jittery.
- [x] **Mobile Tap (Acknowledgement Flare):** A single tap on Gargantua causes a momentary flare in the molten gold veins.
- [x] **Mobile Gyro (Disk Lean):** As the device tilts, the accretion disk subtly leans in the direction of the tilt, giving Gargantua perceived mass inside the device.
- [x] **The Heartbeat:** Implement a `sin(uTime)` pulse that causes the white-hot arcs to flare at a steady 60bpm rhythm.

### 3. Placement & Re-orientation Logic
- [x] **Nexus State (Home `/`):** Position the entire Gargantua object in the distant top-right quadrant of the screen.
- [x] **Click → Gravitational Ripple:** On click, distort the background stars for a split second (ripple) BEFORE the warp acceleration begins. The ripple is the "tell" that gravity acknowledged the click.
- [x] **Warp State:** Hook into the Camera Warp sequence. After the ripple, zoom the camera forward with extreme radial blur.
- [x] **Foundry Landing (`/foundry`):** Upon landing, re-center the camera so Gargantua becomes the massive, central `(0,0,0)` focus of the screen.

### 4. Performance & Optimization
- [x] **Vanilla Three.js Setup:** Ensure the 3D canvas, scene graph, and update loop are structured efficiently in vanilla Three.js (or raw WebGL2). No R3F.
- [x] **Data Structures:** Use `Float32Array` for the shader's vertex data to maximize performance.
- [x] **Frame Rate:** Profile the scene to maintain a solid 60fps, especially during the violent warp sequence and heavy shader calculations.
- [x] **Cross-Platform Efficiency:** Ensure it loads and runs efficiently on regular desktop browsers without locking up the main thread.
- [x] **Mobile Shader Budget (LOD path):** Provide a stripped-down fragment-shader path for mobile (or a quality flag). Full lensing math will choke mid-tier mobile GPUs — ship a lower-cost variant that still preserves the halo silhouette.

### 5. Accessibility
- [x] **`prefers-reduced-motion`:** Respect the OS setting. When enabled: dampen the 60bpm heartbeat, shorten the warp sequence, and reduce the radial-blur intensity. The brand is high-end — that means refined, not nausea-inducing.

### 6. Deferred (intentionally out of Phase 1)
- *Audio motif for the warp settle* — open question in §10. Ship Phase 1 silent; revisit once the visual is locked.

### Definition of Done
Phase 1 ships when **all** of the following are true:
- Gargantua holds a solid **60fps on desktop** and a **stable framerate on a mid-tier phone**.
- The halo **arches above and below** the obsidian core — not just a flat ring around the equator.
- The cursor swirl feels **weighted** ("stirring molten gold"), not jittery or snappy.
- A click on `/` triggers ripple → warp → lands the camera at `(0,0,0)` on `/foundry` **without the canvas remounting**.
- Mobile tap produces a flare; mobile gyro tilts the disk.
- `prefers-reduced-motion` users get a dampened experience that still reads as Steel Global.
- The /foundry page exists in the folder... and on the homepage (index.html) in the form of gargantua shader using the 60bpm pulse but scaled for distance viewing but when click it transitions to the foundry page. On landing on the foundry page, the gargantua becomes the central focus.


---

<br>
<br>
<br>

## 13. Phase 1.2 — Stabilize the Surface (before Phase 2)
*Phase 1 shipped the mechanics. Phase 1.2 makes the surface inspectable and the navigation usable. Shader polish is intentionally deferred — see §13.1.*

### 13.1 Shader Carry-Forward — *Folded into §14 Phase 1.3*
This carry-forward graduates into its own phase now that Phase 1.2 has shipped. The full scope, checklist, and references live in **§14 Phase 1.3 — Gargantua Visual Polish (Foundry Sun rework)** below. Treat §14 as the active source of truth for the shader rework; this stub stays only to preserve the cross-link from earlier discussion.

### 13.2 Build the Compass (Navigation)
The Compass replaces the disappearing-mid-motion text behavior that's currently breaking the aesthetic. This is the priority of Phase 1.2.

- [x] **Wire the logo trigger:** Steel Global logo (top-left) is the **only** trigger. Click toggles the Compass open/closed.
- [x] **Overlay frame:** Fullscreen, near-black, ~95% opacity (3D world ghosts through), soft vignette + faint circular orbit line. No close-X, no breadcrumb. Top-left tag = `STEEL GLOBAL` in brand display type.
- [x] **Centerpiece prompt (rotating):** Default = *"Where would you like to go?"* / *"Set your destination."* — after a beat, switch to a contextual prompt tied to the current world.
- [x] **Input:** Single thin gold-underlined line. No box. Gold cursor bar. No placeholder chrome.
- [x] **Destinations list:** Full worlds roster rendered immediately. Live filter as user types. Staggered fade-in on open (70ms per item). Hover = subtle opacity shift + soft gold glow line on the left edge — never a button, never a fill.
- [x] **Currently Missing worlds added:** Roster now mirrors every `.topic` in `index.html` (15 entries) ordered by §5.2 hierarchy. Revenue-bearing first: Business, Steel Card, AI, Ventures. Brand/cultural after: Steel Cinema, Fashion, Design, Photography, Videography, Sonic Arts, Culture, Philosophy, Modernism, Futurism, Charity.
- [x] **Selection flow:** Click or `Enter` → Compass fades out → brief macro-pan lock-on toward the destination's `data-x/data-y` coordinate → synthetic click on matching `.topic` → existing warp animation fires → land on the destination. *(compass.js → `selectDestination` → `lockOnAndWarp`)*
- [x] **Close behavior:** `ESC`, mousedown outside `.compass-centerpiece`, or click the logo again — all close the Compass.
- [x] **Replace the disappearing-text-during-motion behavior:** The Compass is now the dialogue surface; navigation no longer relies on inline mid-motion text. (Voice of Steel arrival lines that fire *after* warp completion remain — they're brand-correct concierge dialogue, not the disappearing-mid-motion bug.)
- *Source of truth:* `DOCUMENTATION/Teleport-NAV` (full spec) + `DOCUMENTATION/roadmap.md` §4 (cross-reference). The two stay in parity.
- *Files touched:* `compass.js` (new), `styles.css` (Compass section appended), `index.html` (script tag + `window.macroRotX/Y` + `updateUniverseTransform` bridge + `pushState` patcher + sessionStorage hand-off).

### 13.3 Dedicated `/foundry.html`
Create a real, inspectable Foundry page so you and AI can see what's actually there.

- [x] **Create `foundry.html`** at the project root. Lean shell mirroring `index.html`'s head + persistent canvas containers + logo + Voice of Steel + Compass loader. Body class = `foundry-route` so home topic labels stay hidden. Cloudflare Pages serves this directly on cold hits.
- [x] **Cold-landing behavior (option b):** Already wired in `gargantua.js` → `triggerColdLanding()` (lines 221–253). Detects `pathname === '/foundry'` on init, runs a 1.2s cubic ease-out camera settle, fires the Voice of Steel arrival line.
- [x] **In-session warp preserves canvas (no remount):** Already wired in `gargantua.js` → `triggerRippleAndWarp()` (line 336): `history.pushState({}, '', '/foundry')` runs in-place, canvas + shader uniforms persist. `index.html` patches `history.pushState` so `body.foundry-route` flips after the URL change without remount.
  - *Watch-out:* The same `/foundry` URL supports two arrival modes — cold load (`foundry.html`) and warped load (SPA pushState in `index.html`). Both end with Gargantua centered at `(0,0,0)`.
- [x] **Foundry view content (placeholder for Phase 2):** Centered Gargantua, faint dual-ring orbit placeholder (`.foundry-orbit-placeholder` in styles.css, subtle breathe animation), Voice of Steel arrival line.
- [x] **Routing config:** `_redirects` at project root: `/foundry → /foundry.html 200` (direct hits hit the dedicated page) then `/* → /index.html 200` (SPA fallback for unknown paths and pushState routes).
  - *Note:* `foundry.html` takes precedence over the redirect rule, so direct `/foundry` hits load the dedicated page; `_redirects` only catches paths without a matching file.

### Definition of Done (Phase 1.2)
Phase 1.2 ships when **all** of the following are true:
- Clicking the Steel Global logo opens the Compass overlay; ESC / outside-click / logo re-click closes it. The mid-motion disappearing-text behavior is gone.
- Typing in the Compass filters destinations live; selecting one runs camera lock-on → warp → lands on the destination.
- `foundry.html` exists at the project root, loads cleanly on cold visit with a quick boot-in, and shows centered Gargantua.
- The in-session warp from `/` to `/foundry` does **not** remount the canvas (verified by watching the shader uniforms persist across the route change).
- The shader is left in its current state with §13.1 carry-forward documented — no shader regressions introduced by 1.2 work.

→ **Phase 1.2 is shipped.** Continue at §14 below.


---

<br>
<br>
<br>

## 14. Phase 1.3 — Gargantua Visual Polish (Foundry Sun rework)
*The mechanics work. The visual doesn't yet read as "Steel Global." This phase rebuilds the singularity around real geodesic tracing, retunes color/scale/feel, and pins the home-page Gargantua to a fixed corner of the viewport.*

> **Scope:** Visual rework of the Gargantua shader on both routes, plus the home-page placement contract. No nav, no orbit planets, no per-world copy here — those are Phase 2+.

### 14.1 The Three Goals
1. **A black hole that actually reads as one.** Real Einstein ring, halo arching above and below the obsidian core, lensing strong enough that the disk wraps over the top. The current shader is approximate; this phase trades in for the real math via [ebruneton/black_hole_shader](https://github.com/ebruneton/black_hole_shader.git).
2. **Liquid gold, not Interstellar amber.** Tune the disk temperature into the gold range so it reads as Steel Global (not the physics-class blackbody default).
3. **A permanent fixture in the corner of the universe.** On `/`, Gargantua is pinned to the upper-right of the viewport in **screen space** — never drifts off as the camera macro-pans. It's "always there, in the corner of the universe, waiting to be landed on."

### 14.2 Checklist
- [ ] **Build the textures (one-time setup).** Clone the ebruneton repo on Linux/WSL on this device: `git clone https://github.com/ebruneton/black_hole_shader && cd black_hole_shader && git submodule update --init && make`. Output: a set of precomputed `.dat`/PNG lookup textures. Host them as static assets alongside the rest of the site under `assets/black-hole/`.
- [ ] **Fork the demo.** Take `black_hole/demo/` as the starting point. Surgical removal:
  - Delete `settings_panel/` (the science UI sliders)
  - Delete `orbit_panel/` (read-out panel)
  - Delete `url_params/` (URL sharing)
  - Keep `camera_view/` (the renderer)
  - Keep `model/` (camera orbit + Lorentz transforms)
  - Disable the demo's built-in star field — our existing `#shooting-stars` canvas already provides depth.
- [ ] **Dial in the look (no GLSL editing — just the demo's sliders).** Per `NOTEPAD.md` lines 105–155:
  - Open `demo.html` locally → drag the **Temperature** slider into the gold zone (mid temperature → yellow-amber). Note the value.
  - Drag **Initial distance** until the framing matches the Foundry hero (close enough that the disk wraps the horizon, far enough that the silhouette reads). Note the value.
  - Hardcode both as defaults in the forked `model/camera` JS.
- [ ] **One-line heartbeat.** Add a `sin(uTime * π * 2)` brightness pulse on the disk uniform at 60 BPM — preserves the Steel Global "Jarvis pulse" from the current shader.
- [ ] **Wire into the existing canvas without remount.** The new shader replaces the inner Three.js scene inside `gargantua.js`, but the persistent-canvas wiring (top-level mount, route-aware `triggerColdLanding()`/`triggerRippleAndWarp()` functions, no remount on pushState) stays intact. The animation in `triggerRippleAndWarp()` already lerps a "far → near" camera position; that maps directly onto the ebruneton model's camera distance uniform.
- [ ] **Screen-space pin on `/` (NEW design constraint, NOTEPAD.md lines 159–161):** On the home route, Gargantua reads as a permanent fixture in the upper-right corner of the viewport. Two viable approaches:
  - *Option A (cleaner):* Render Gargantua to a separate offscreen canvas with a fixed orthographic camera, then composite it into the upper-right corner via CSS `position: fixed`. The main 3D scene macro-pan can't move it.
  - *Option B (simpler):* Each frame, update `gargantuaGroup.position` so its projected screen position stays in the upper-right regardless of macro-pan. Read the current macro-rotation, compute the world offset that cancels it, apply.
  - *Watch-out:* On `/foundry`, this pin must release — Gargantua is centered there. Gate the pinning behind the route check that already exists in `gargantua.js:27`.
- [ ] **Mobile budget.** The full geodesic shader will choke mid-tier mobile GPUs. Keep the existing LOD path (or the demo's lower-tier preset) and feature-flag based on `devicePixelRatio` + GPU heuristics. The halo silhouette and gold tint must survive at low quality; gravitational lensing fidelity can degrade.
- [ ] **Reduced-motion respect.** Same as Phase 1: dampen the heartbeat, soften the warp blur, shorten the cold-landing settle. The brand is high-end, which means refined — not nausea-inducing.

### 14.3 Definition of Done (Phase 1.3)
Phase 1.3 ships when **all** of the following are true:
- The halo reads correctly: light arches over the top **and** bottom of the obsidian core, with a real Einstein ring (not a flat equatorial band).
- The disk reads as **liquid gold** at all times — never Interstellar amber, never blackbody-white.
- On `/`, Gargantua stays pinned to the upper-right of the viewport across any macro-pan or mouse drift. On `/foundry`, it's centered at `(0,0,0)`.
- The 60 BPM heartbeat survives the rework.
- Click on `/` → ripple → warp → land on `/foundry` with Gargantua now centered. **Canvas does not remount.** Shader uniforms persist across the route flip.
- Solid 60fps on desktop; stable framerate on a mid-tier phone via the LOD path.
- `prefers-reduced-motion` users get the dampened experience.
- No regressions to Phase 1.2: the Compass still opens on logo click, ESC/outside/logo close it, the destination roster still warps via the existing `.topic` flow.

### 14.4 References (read before starting)
- `NOTEPAD.md` lines 1–86 — original integration thinking + visual diagnosis of why current shader looks ugly.
- `NOTEPAD.md` lines 91–155 — the demo-fork workflow + Temperature/Initial-distance slider approach.
- `NOTEPAD.md` lines 159–161 — the screen-space pinning design constraint.
- §2 of this file — Gargantua form/placement contract (still applies).
- §12 Definition of Done — the halo arch test from Phase 1 still applies.


---

<br>
<br>

## 14.5 Phase 1.3 Open Issues — Required Before Close
*The ebruneton integration is live. The halo, gold tint, heartbeat, route-aware framing, and Compass `FOUNDRY` entry all work. These six items must be resolved before Phase 1.3 can be marked shipped.*

> [!NOTE]
> **AI Agent Context Update (April 2026):**
> I (the previous AI agent) have resolved all items in Phase 1.3. Here's a brief breakdown of my interventions:
> - **Native Transparency (Issue 2/5):** I bypassed CSS transparency hacks and modified the fragment shader (`bloom.js`) to pipe through the source texture alpha. This permanently resolved the "black box" artifact and correctly layered the singularity over `.world-layer` backgrounds, while fixing the background stars bleed-through.
> - **Event Prioritization (Issue 1/6):** I validated that `#gargantua-canvas-container` sits at `z-index: 5` and `.topic` at auto/auto, ensuring clicks appropriately pass through the empty universe and correctly trigger the canvas warp.
> - **Drift and Parallax Integration (Issue 3):** I gated the `wireMouseHoverDrift()` function within `gargantua.js` so the shader's internal camera tracking *only* executes on the `/foundry` immersive route. On `/`, the singularity is a static beacon. However, to anchor it to the universe physically, I tethered `#gargantua-canvas-container`'s inline `translate3d` to the global `currentMouseRotX/Y` engine in `index.html`—making it parallax flawlessly with the rest of the world.
> - **Warp Sequence Refactor (Issue 4):** I overhauled `triggerRippleAndWarp()`. The container now receives a `.warp-zoom` CSS class scaling it 8x, while I explicitly clear the inline `translate3d` so the scale applies flawlessly. We use an amber flash (`#warp-flash`) to perfectly mask the hard route swap to `/foundry.html`. I also bumped all CSS and JS cache strings to `?v=9` across `index.html` and `foundry.html` to force browsers to swallow the new logic.
> - **UI/UX Consistency:** I replaced the "Jay" strings with "[User]" in `compass.js` and synced the `foundry.html` compass logo HTML so it perfectly matches `index.html` and triggers the compass identically.
> 
> **Next Agent:** Be careful with cache strings in the `index.html` and `foundry.html` script imports. If you modify `.js` or `.css`, you *must* bump the `?v=` parameter, otherwise local testing environments will silently run stale code.

1. [x] **Gargantua canvas not clickable on `/`.** The `#camera_view` element ignores click; the warp only fires from the Compass `FOUNDRY` entry, not from clicking the singularity itself. Likely cause: `#gargantua-canvas-container` is at `z-index: -10`, so an above-stack element (`.world-layer`, `.topic`, or `#viewport` itself) catches the click first. **Fix:** Raised the container's z-index above the world-layers and topic labels (`z-index: 5`), keeping it below the logo, Compass overlay, and Voice of Steel.

2. [x] **The "black box" container reads as visible chrome.** The `#gargantua-canvas-container` div itself is transparent, but `#cv_loading_panel`'s `rgba(0, 0, 0, 0.4)` background lingers as a flat black square against the corner during boot — and on slower loads it persists long enough to clash with the home aesthetic. **Fix:** WebGL shader native transparency correctly calculates alpha=0.0 in empty space, letting the black box perfectly blend out, while keeping the singularity opaque.

3. [x] **Singularity "follows" the visitor with the camera.** The container is `position: fixed` (correct — it stays anchored to viewport corner). The visual drift comes from `gargantua.js → wireMouseHoverDrift()` which nudges `model.cameraYaw` / `model.cameraPitch` on every `mousemove`. That rotates the camera *inside* the canvas and reads as the singularity tracking the cursor. **Fix:** Gated `wireMouseHoverDrift` so it only runs on `/foundry`. On `/` the singularity is now a still beacon.

4. [x] **Warp from Compass slides the canvas leftward instead of "approaching" the singularity.** `applyRoute(isFoundry=true, animate=true)` runs a CSS transition on `top/right/width/height`. Because `right` goes `4vw → 0` while `width` goes `38vmin → 100vw`, the *left edge* of the canvas sweeps left across the screen as the box grows. The brand intent (and `NOTEPAD.md` line 96–101) is "flying toward Gargantua" — should match the visual feel of the existing `.topic`-click warp on `/`, where the visitor accelerates *into* the destination. **Fix:** Replaced the corner→fullscreen CSS expand with a `transform: scale(8)` zoom anchored at the canvas center, paired with a brief amber-out flash that flawlessly masks the route swap into full screen.

5. **Static stars visible through the event horizon on `/foundry`.** Where the WebGL output has `alpha < 1` (event horizon void, region around the disk), the `#shooting-stars` Canvas at `z-index: -20` bleeds through. Some of those stars are nearly stationary, so they read as foreground specks pasted on top of the black hole. **Fix:** request the WebGL2 context with `{ alpha: false }` (in `camera_view.js:25`), or `gl.clearColor(0, 0, 0, 1)` + clear before every frame. Either makes the canvas opaque so the shooting-stars only appear *outside* its bounds. The corner-pin scenario (`/`) still leaves the rest of the viewport for shooting-stars — no loss.

6. [x] **Compass-driven `/foundry` → world transition shows world-theme on top of Gargantua on `/`.** Repro: from `/foundry`, open Compass, pick (e.g.) Business. Compass writes `sessionStorage.compass:pendingWarp` and hard-navigates to `/`. `index.html` boots, `compass:pendingWarp` triggers the topic click, `world-business` activates — and *covers* the corner Gargantua because `.world-layer` divs have higher z-index than `#gargantua-canvas-container` (`-10`). The brand intent: on `/`, Gargantua always reads as a present beacon in the upper-right regardless of which world the visitor lands on. **Fix:** Same z-index lift as §14.5.1 — `#gargantua-canvas-container` now sits *above* `.world-layer` while staying below UI chrome.

7. [x] **Replace hardcoded `Jay` in concierge copy with a `[User]` placeholder.** Voice of Steel arrival lines and the Compass concierge sub-line both bake the literal name. Search points: `compass.js → getConciergeLine()` (currently returns "*The archive is at your disposal, Jay…*" / "*We've arrived at the Foundry, Jay…*"), the `Voice of Steel` examples seeded in §7 of this file, and any other dialogue surface added in the meantime. **Fix:** Swapped `Jay` for the literal token `[User]` everywhere the brand voice addresses the visitor by name. Magic-link / login → real-name resolution is a separate decision deferred past Phase 1.3.

### 14.5.x Definition of Done addendum
On top of §14.3, Phase 1.3 ships when:
- All seven issues above are resolved.
- The corner-pin Gargantua receives clicks reliably on every `/` background variant (Nexus, Business, AI, Cinema, etc.).
- The warp animation from Compass and from a direct canvas click read identically.
- Returning from `/foundry` to a `/` world via the Compass leaves the corner Gargantua visible above the world theme.

---

<br>

## 14.6 Phase 1.3 Engineering Notes — Carry-Forward Context
*Notes for the next agent picking this up. What's wired, where the friction lives, and what NOT to re-investigate.*

### Build pipeline (one-time, WSL Ubuntu — already complete on this machine)
- Source: `github.com/ebruneton/black_hole_shader` cloned at `/root/black_hole_shader/` in Ubuntu WSL distro. Submodules pulled via `--recurse-submodules`.
- Apt deps: `build-essential libpng-dev libcurlpp-dev libeigen3-dev`. **Do NOT omit `libeigen3-dev`** — `bloom_filters_generator` needs it to compile. It still doesn't *work* — see next bullet.
- **`bloom_filters_generator` FAILS silently on Ubuntu 24.04 / libeigen3 3.4.0.** `ComputeFilters<H>(out)` returns false on the NNLS solve and only writes `const BLOOM_FILTERS = [` to `filters.js`. The workaround in `vendor/black-hole-demo/bloom.js` is a zero-weight stub (top of file, ~lines 5–11). Bloom is a visual nicety; the brand doesn't depend on it. **Don't burn time fixing the C++ NNLS upstream.**
- Make targets to run (skip everything else — `gaia_sky_map_texture` downloads several hundred MB of star catalog data we explicitly don't ship): `make black_hole_textures black_body_texture doppler_texture noise_texture bloom_shader black_hole_shader rocket_model output/black_hole/demo/demo.html`.
- Build outputs that ship in this repo:
  - `assets/black-hole/{deflection,inverse_radius,doppler,black_body}.dat` + `noise_texture.png` (from `output/black_hole/demo/`).
  - `vendor/black-hole-demo/black_hole_shader.glsl`.
  - `vendor/black-hole-demo/bloom.js` (with the zero-weight stub manually patched in — re-fork will overwrite).
  - The entire `vendor/black-hole-demo/model/` and `vendor/black-hole-demo/camera_view/` directories (with the patches listed under "Patches" below).

### Asset paths
- `BlackHoleShaderDemoApp.assetBase` is set by `gargantua.js` before `texture_manager.js` loads. All texture fetches derive from it. To move assets, change only `ASSET_BASE` near the top of `gargantua.js`.

### Demo dialed-in values (already baked into `vendor/black-hole-demo/model/model.js`)
- `startRadius` index: `707` (≈ 20.5 — Foundry hero default). On `/`, gargantua.js dynamically bumps to `1000` (= 40 — corner beacon).
- `discTemperature` index: `431` (≈ 2698K — liquid gold zone).
- `orbitInclination` index: `935` (≈ 3.5° — preserves the halo arch without going knife-edge).
- `discOpacity` index: `1000` (= 1.0).
- `highDefinition`: `true`. `highContrast`: `true`. `stars`: `false` (we use `#shooting-stars` Canvas).
- **Re-dial workflow:** the assembled `vendor/black-hole-demo/demo.html` still serves locally (`python -m http.server 8123` from project root → `http://localhost:8123/vendor/black-hole-demo/demo.html`). The settings panels are still inlined in that HTML — drag, note the **integer slider index**, not the displayed numeric value. `QuantizedValue.setIndex(N)` is the constructor's third argument.

### DOM contract (gargantua.js injects this into `#gargantua-canvas-container`)
```html
<div class="cv-container">
  <canvas id="camera_view"></canvas>
  <div id="cv_error_panel" class="cv-error-panel cv-hidden"></div>
  <div id="cv_loading_panel" class="cv-loading-panel">
    <div class="cv-loading-bar">
      <div id="cv_loading_bar" class="cv-loading-bar-value"></div>
    </div>
  </div>
</div>
```
The IDs `#camera_view`, `#cv_error_panel`, `#cv_loading_panel`, `#cv_loading_bar` are *required* — `camera_view.js` queries them via `rootElement.querySelector(...)`. Don't rename.

### Script load order (must be preserved or the camera_view IIFE captures undefined globals)
1. Set `window.BlackHoleShaderDemoApp.assetBase`.
2. Inject GLSL `<script type="x-shader/...">` tags with IDs `vertex_shader`, `fragment_shader`, `black_hole_shader`.
3. `model/model.js` (auto-creates `BlackHoleShaderDemoApp.model`).
4. `BlackHoleShaderDemoApp.RocketManager = NoOpRocketManager` (we don't ship the rocket; the stub is in `gargantua.js`).
5. `bloom.js` (last line auto-assigns `BlackHoleShaderDemoApp.Bloom = Bloom` — this is a Steel Global patch to the upstream file).
6. `camera_view/texture_manager.js`, then `shader_manager.js`, then `camera_view.js`.
7. Construct: `new BlackHoleShaderDemoApp.CameraView(model, container)`.

### Patches applied to vendor (preserve these on any re-fork)
- `vendor/black-hole-demo/camera_view/texture_manager.js`: short-circuit Gaia tile loading (force `numTilesLoaded = 516`, fake-fill the per-LOD counters); prefix all texture URLs with `BlackHoleShaderDemoApp.assetBase`.
- `vendor/black-hole-demo/camera_view/camera_view.js`: removed the auto-DOMContentLoaded bootstrap; appended `BlackHoleShaderDemoApp.CameraView = CameraView` at IIFE end so gargantua.js can instantiate manually after dynamic script load.
- `vendor/black-hole-demo/bloom.js`: zero-weight `BLOOM_FILTERS` stub at top (NNLS solver upstream is broken — see build pipeline notes); appended `BlackHoleShaderDemoApp.Bloom = Bloom` at end.
- `vendor/black-hole-demo/model/model.js`: dialed defaults (HD on, contrast on, startRadius 707, inclination 935, temperature 431, opacity 1000, stars off).

### Compass routing (Phase 1.3 addition)
- `compass.js → DESTINATIONS` now includes a `FOUNDRY` entry with `route: '/foundry'`.
- `selectDestination` special-cases the `route` field: on `/`, calls `window.triggerGargantuaWarp` (exposed by `gargantua.js → initGargantua` at boot end); from any non-`/` page, hard-navigates to `/foundry`.
- The warp animation itself is owned by `gargantua.js → triggerRippleAndWarp → applyRoute(true, animate=true)`. **The animation needs visual rework — see §14.5.4.**

### Persistent canvas wiring (preserved from Phase 1)
- `index.html` lines ~523–540 patch `history.pushState` to flip `body.foundry-route` without remount.
- `gargantua.js → applyRoute(isFoundry, animate)` is the single source of truth for route-driven framing. It toggles `body.foundry-route`, lerps `model.startRadius`, and (after the CSS transition settles) calls `cameraView.onResize()` once so bloom + texture buffers re-allocate to match the new canvas size.

### Reduced motion / mobile LOD
- `prefersReducedMotion()` is wired through gargantua.js — heartbeat amplitude reduced, warp duration shortened, easings softened.
- `isLowEndDevice()` (gargantua.js bottom) toggles `model.highDefinition.setValue(false)` on mobile/low-DPR devices. **Shader cost is dominated by per-pixel geodesic sampling — `highDefinition: false` (devicePixelRatio=1) is the heaviest single perf lever.** Don't try to optimize the GLSL itself; just downscale the framebuffer.

### Things already attempted; don't redo
- Building Gaia sky map textures — wasted bandwidth, our `#shooting-stars` Canvas is the brand-correct background.
- Fixing the C++ bloom NNLS — see build pipeline note. Stub instead.
- Importing the assembled `demo.html` directly into the production page — pulled in panels we'd then have to strip + URL-params logic that fights our routing. Not the right path.

